@startuml
start

if (Registered) then (true)
else (false)
	:Profile(name, email, Availabilitie(date, period));
endif
while (searchStudent(section)) is (found a student)
	if (studentFound.getAvailabilities()) then (available)
		:sendMessage(studentFound, message);
		if (answer of student) then (ok)
			:Work together;
			end
		else (not ok)
		endif
	else (not available)
	endif
endwhile (not found a student)
stop
@enduml
